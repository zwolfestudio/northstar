function Settings() {
  return (
    <div>
      <h1>Settings</h1>
      <p className="hint">
        Settings aren't scoped yet. This page is a placeholder until there's something to
        configure.
      </p>
    </div>
  );
}

export default Settings;
